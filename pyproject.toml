[project]
name = "walmart-project-dbt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shourya845", email = "shouryanegi9206@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "apache-airflow>=3.2.2",
    "databricks-sdk>=0.117.0",
    "dbt-core>=1.12.0",
    "dbt-databricks>=1.12.3",
]

[project.scripts]
walmart-project-dbt = "walmart_project_dbt:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
